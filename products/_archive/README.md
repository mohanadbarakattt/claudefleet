# Archived products (retired 2026-07-25)

The storefront was consolidated from four small products into **two machines**
(`autopilot-content-machine`, `digital-product-machine`) built on a shared
`_engine`. These four are no longer sold or listed on the site.

They are kept because the content is good and still gets used — the best of it
ships as **bonus material inside the machines** rather than as separate SKUs:

| Archived product | Where it lives now |
|---|---|
| `prompt-vault` (50 money prompts) | Bonus inside the **Autopilot Content Machine** — the content-creation and freelance-services categories complement the SCRIPT and ASSEMBLE stages. |
| `claude-money-os` (8-chapter playbook) | Bonus inside the **Digital Product Machine** — the offer, delivery and 90-day-plan chapters back up the VALIDATE and PACKAGE stages. |
| `claude-power-pack` (5 agents + 7 commands) | Superseded. The machines ship their own agents and skills, purpose-built per pipeline stage. Kept for reference only. |
| `claudefleet-machines` (3 business playbooks, $97) | Superseded by the two real machines, which ship working software (admin panel, hooks, state model) instead of playbooks alone. |

**Do not re-list these on the site.** If you ever want to sell one again, treat
it as a new decision — don't just un-archive it, because the pricing, the
positioning and the honesty language have all moved on since.
